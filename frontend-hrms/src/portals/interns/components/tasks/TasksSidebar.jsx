export default function TasksSidebar({ allComments, commentText, onCommentChange, onPostComment, onShowDetail, selectedTask, viewMode }) {
  return (
    <div className="space-y-5"><div onClick={onShowDetail} className="bg-white p-5 rounded-xl border border-gray-200 shadow-sm cursor-pointer"><h3 className="font-semibold mb-3">Task Details</h3>{viewMode === 'list' ? <><p className="font-bold">{selectedTask.title}</p><p className="text-sm text-gray-500 mt-2">{selectedTask.description}</p><p className="text-xs mt-3">Start: {selectedTask.start}</p><p className="text-xs">Due: {selectedTask.due}</p></> : <><p className="text-sm">Type: {selectedTask.type}</p><p className="text-sm">Start: {selectedTask.start}</p><p className="text-sm">Due: {selectedTask.due}</p><p className="text-sm">Submitted: {selectedTask.submitted || 'None'}</p></>}</div><div className="bg-white p-5 rounded-xl border border-gray-200 shadow-sm"><h3 className="font-semibold mb-3">Comments</h3>{allComments.length > 0 ? allComments.map((comment) => <div key={comment.id} className="mb-3"><p className="text-sm font-medium">{comment.author}</p><p className="text-sm text-gray-500">{comment.message}</p></div>) : <p className="text-gray-400 text-sm">No comments</p>}<textarea value={commentText} onChange={(event) => onCommentChange(event.target.value)} className="w-full border border-gray-300 rounded p-2 mt-3" placeholder="Write a comment..." /><button onClick={onPostComment} className="mt-2 bg-purple-500 text-white px-3 py-1 rounded">Post</button></div>{viewMode === 'list' ? <div className="bg-white p-5 rounded-xl border border-gray-200 shadow-sm"><h3 className="font-semibold mb-2">{selectedTask.progress}% Completed</h3><div className="w-full bg-gray-200 h-2 rounded"><div className="bg-purple-500 h-2 rounded" style={{ width: `${selectedTask.progress}%` }} /></div></div> : null}</div>
  );
}