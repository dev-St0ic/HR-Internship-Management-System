import EmployeeAvatar from '../../staff-management/EmployeeAvatar';

export default function InternManagementRequestRow({ request, onOpenRequestModal, viewIcon, editIcon }) {
  return (
    <tr><td className="intern-management-cell-name"><div className="document-vault-person-cell document-vault-person-cell-with-avatar intern-management-person-cell-with-avatar"><EmployeeAvatar src={request.internAvatar} alt={request.internName} name={request.internName} size={30} className="document-vault-person-avatar" /><span title={request.internName}>{request.internName}</span></div></td><td className="intern-management-cell-date">{request.dateLabel}</td><td className="intern-management-cell-issue-type">{request.issueType}</td><td className="intern-management-cell-date">{request.requestedAtLabel}</td><td className="intern-management-cell-status"><span className={`intern-management-status-pill is-${request.workflowStatus.toLowerCase()}`}>{request.workflowStatus}</span></td><td className="intern-management-cell-action"><div className="document-vault-actions intern-management-row-actions"><button type="button" className="document-vault-action-button intern-management-icon-button" onClick={() => onOpenRequestModal(request, 'view')} aria-label={`View ${request.internName} attendance request`}><img src={viewIcon} alt="" aria-hidden="true" className="intern-management-action-icon" /></button><button type="button" className="document-vault-action-button intern-management-icon-button" onClick={() => onOpenRequestModal(request, 'edit')} aria-label={`Edit ${request.internName} attendance request`}><img src={editIcon} alt="" aria-hidden="true" className="intern-management-action-icon" /></button></div></td></tr>
  );
}