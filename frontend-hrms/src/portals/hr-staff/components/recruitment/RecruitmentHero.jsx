import { Building2, ShieldCheck, Users } from 'lucide-react';

export default function RecruitmentHero({ topPartnerUniversity, latestApplication, applicationQueues, partnerUniversities, overviewMetrics, onHighlightClick }) {
  return (
    <section className="recruitment-hero"><div className="recruitment-hero-main"><span className="recruitment-eyebrow">Talent Pipeline</span><h3>{topPartnerUniversity ? `${topPartnerUniversity.name} currently leads the internship pipeline.` : 'Recruitment data is ready to review.'}</h3><p>{topPartnerUniversity && latestApplication ? `${topPartnerUniversity.applicantCount} applicants from ${topPartnerUniversity.name} are represented across ${topPartnerUniversity.departmentCount} departments, with ${applicationQueues['for-admin-approval'].length} records already waiting for admin approval. The latest submission on file is ${latestApplication.name}.` : 'Use this page to review applications, approval-ready profiles, and partner university activity.'}</p><div className="recruitment-hero-highlights"><button type="button" className="recruitment-hero-highlight" onClick={() => onHighlightClick('applications')}><Users size={16} strokeWidth={2} />{latestApplication ? `Latest applicant: ${latestApplication.name}` : 'No applicants yet'}</button><button type="button" className="recruitment-hero-highlight" onClick={() => onHighlightClick('for-admin-approval')}><ShieldCheck size={16} strokeWidth={2} />{applicationQueues['for-admin-approval'].length} ready for admin review</button><button type="button" className="recruitment-hero-highlight" onClick={() => onHighlightClick('partner-university')}><Building2 size={16} strokeWidth={2} />{partnerUniversities.length} active university partners</button></div></div><div className="recruitment-metric-grid">{overviewMetrics.map((metric) => { const Icon = metric.icon; return <article key={metric.key} className="recruitment-metric-card"><div className="recruitment-metric-icon"><Icon size={18} strokeWidth={2.2} /></div><div><span className="recruitment-metric-label">{metric.label}</span><strong className="recruitment-metric-value">{metric.value}</strong><p>{metric.detail}</p></div></article>; })}</div></section>
  );
}